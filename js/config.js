// config.js — All environment-specific constants.
// Replace API_BASE_URL after deploying pega-form-backend to Vercel.

const CONFIG = {
  API_BASE_URL:        "https://YOUR-VERCEL-APP.vercel.app",
  API_SUBMIT_ENDPOINT: "/api/submit",
  API_TIMEOUT_MS:      15000,
};

export default CONFIG;
